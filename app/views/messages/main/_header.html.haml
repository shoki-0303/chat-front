.header
  %h2.header__groupname
    = @group.name
  %ul
    %h3.header__member member:
    %li.header__member__list
      - @group.users.each do |user|
        = user.name
  = link_to "Edit", edit_group_path(@group), {:class => "header__edit-link"}
